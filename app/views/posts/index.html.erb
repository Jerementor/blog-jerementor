<div class="content-box w-richtext">
  <% @posts.each do |post| %>
    <h2><%= link_to(post.title, (post)) %></h2>
    <br>
    <%= post.subtitle %>
  <% end %>


<%= link_to 'New Post', new_post_path %>
</div>

